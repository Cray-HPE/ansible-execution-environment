@Library('dst-shared@master') _
 
dockerBuildPipeline {
    repository = "cray"
    imagePrefix = "cray"
    app = "aee"
    name = "aee"
    description = "Ansible Execution Environment"
    receiveEvent = ["csm-ssh-keys"]
    product = "csm"
    }
